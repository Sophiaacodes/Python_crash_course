for value in range(1,6):
	print(value) #it always prints one number less than the last one you give it

even_numbers = list(range(2, 11, 2))
print(even_numbers)

odd_numbers = list(range(1, 11, 2)) #the last number 2 is the one that tells py what to skip (in this case every second number)
print(odd_numbers)

q3d = list(range(1, 21, 3)) #in this case every third number -> this can be useful to set timers and stuff!!!!
print(q3d)